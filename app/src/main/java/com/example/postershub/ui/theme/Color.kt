package com.example.postershub.ui.theme

import androidx.compose.ui.graphics.Color

// Cinematic dark palette
val Ink = Color(0xFF07070B)          // app background (near-black)
val InkElevated = Color(0xFF121218)  // surface
val InkCard = Color(0xFF1B1B24)      // cards
val Electric = Color(0xFF7C5CFF)     // primary accent
val ElectricSoft = Color(0xFFB9A6FF)
val Gold = Color(0xFFF5C518)         // secondary accent (rating)
val Mist = Color(0xFFC9C9D6)         // muted text
val OnInk = Color(0xFFF3F3F8)

// Legacy names kept so the default generated theme references still resolve if reused.
val Purple80 = ElectricSoft
val PurpleGrey80 = Mist
val Pink80 = Gold
val Purple40 = Electric
val PurpleGrey40 = InkCard
val Pink40 = Gold
